import PageSelector from './components/PageSelector';

/**
 * App Component
 * Root component that renders the PageSelector
 * This is the main entry point for the application
 */
export default function App() {
  return <PageSelector />;
}